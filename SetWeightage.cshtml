@model IEnumerable<Flex.Models.FacultySectionSemesterCourses>

@{
    ViewData["Title"] = "SetWeightage";
}

<h1>Edit</h1>

<h4>FacultySectionSemesterCourses</h4>
<hr />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SectionSemesterCourse.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SectionSemesterCourse.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SemCourses.Course.courseName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SectionSemesterCourse.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SectionSemesterCourse.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SemCourses.Course.courseName)
                </td>
                <td>
                    <a asp-action="EditWeightage" asp-route-id="@item.Id">Edit Weightage</a>
                </td>
            </tr>
        }
    </tbody>
</table>




@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
